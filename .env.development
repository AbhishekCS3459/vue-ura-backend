# Development environment - MariaDB running in Docker (docker-compose)
# DB_HOST=127.0.0.1 for local artisan commands; Docker overrides to 'mariadb' in container
APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000
APP_TIMEZONE=UTC

# Docker MariaDB credentials (from docker-compose.yml)
DB_CONNECTION=mariadb
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=vue_ura_db
DB_USERNAME=vue_ura_user
DB_PASSWORD=vue_ura_password

CACHE_DRIVER=file
SESSION_DRIVER=file
QUEUE_CONNECTION=sync
